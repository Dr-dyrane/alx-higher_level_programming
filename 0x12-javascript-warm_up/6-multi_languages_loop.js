#!/usr/bin/node

/**
 * Script that prints three lines using an array and a loop.
 * @module 6-multi_languages_loop
 */

for (const line of ['C is fun', 'Python is cool', 'JavaScript is amazing']) {
  console.log(line);
}
